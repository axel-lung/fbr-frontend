import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.alng.footbr',
  appName: 'Football BR',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
